name: Publish to npm

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  publish:
    name: Publish Package to npm
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          # REQUIRED for Token Auth: This creates the .npmrc file that reads NODE_AUTH_TOKEN
          registry-url: "https://registry.npmjs.org/"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package
        run: pnpm build

      # 1. Parse the Git tag to determine the npm tag
      - name: Determine npm dist-tag
        id: disttag
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          echo "Detected git tag: $TAG"
          
          if [[ "$TAG" == *"-beta"* ]]; then
            echo "tag=beta" >> $GITHUB_OUTPUT
          elif [[ "$TAG" == *"-rc"* ]]; then
            echo "tag=rc" >> $GITHUB_OUTPUT
          else
            echo "tag=latest" >> $GITHUB_OUTPUT
          fi

      # 2. Sync package.json version with Tag
      - name: Sync package.json version with Tag
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Setting package.json version to: $VERSION"
          npm version $VERSION --no-git-tag-version --allow-same-version

      # 3. Publish using Token
      - name: Publish to npm
        env:
          # This now works because registry-url above configured .npmrc to look for it
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm publish --provenance --access public --tag ${{ steps.disttag.outputs.tag }}